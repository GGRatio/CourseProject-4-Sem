<UserControl x:Class="Energy.Controls.HeaderControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Energy.Controls"
             mc:Ignorable="d" 
             d:DesignHeight="1920" d:DesignWidth="260">
    <Grid Background="LightGray">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>  <!-- Разделитель -->
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.Resources>
            <Style TargetType="Button">
                <Setter Property="Padding" Value="40,10 "></Setter>
                <Setter Property="Margin" Value="5,0"></Setter>
                <Setter Property="VerticalAlignment" Value="Center"></Setter>
                <Setter Property="BorderBrush" Value="Transparent"></Setter>
                <Setter Property="Background" Value="Transparent"></Setter>
                <Setter Property="BorderThickness" Value="0"></Setter>
                <Setter Property="FontSize" Value="18"></Setter>
                <Setter Property="HorizontalAlignment" Value="Left"></Setter>

                <Style.Triggers>
                    <Trigger Property="IsFocused" Value="True">
                        <Setter Property="FontWeight" Value="SemiBold"></Setter>
                    </Trigger>
                </Style.Triggers>
            </Style>
        </Grid.Resources>
        
        <StackPanel Orientation="Vertical" VerticalAlignment="Top"  
                    HorizontalAlignment="Center" Grid.Row="0">
            <Button x:Name="btn_Groupe" Content="Групповые занятия" Click="btn_Groupe_Click"/>
            <Button x:Name="btn_Trainers" Content="Тренеры" Click="btn_Trainers_Click"/>
            <Button x:Name="btn_Subscriptions" Content="Абонементы" Click="btn_Subscriptions_Click"/>
        </StackPanel>

        <!-- Разделительная черта -->
        <Border Grid.Row="1" 
                Height="1" 
                Background="Gray" 
                Margin="20,10,20,10"/>

        <StackPanel Orientation="Horizontal" VerticalAlignment="Top"  
                    HorizontalAlignment="Center" Grid.Row="2"
                    Margin="0,0,0,60">
          <!--Сюда аватарку добавтить-->
            <Button x:Name="btn_Profile" Content="Профиль" Click="btn_Profile_Click"/>
        </StackPanel>
    </Grid>
</UserControl>
 
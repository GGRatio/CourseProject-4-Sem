<Window x:Class="Energy.Registration"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Energy"
        mc:Ignorable="d"
        Title="Registration" Height="600" Width="1000" Background="#ffdadada">
    <Grid>
        <Border MinWidth="400" Margin="30" Background="White"
                VerticalAlignment="Center" Padding="30" MaxWidth="600">
            <Border.Effect>
                <DropShadowEffect BlurRadius="10" Color="LightGray" ShadowDepth="5"/>
            </Border.Effect>
            <StackPanel>
                <TextBlock Text="Регистрация" FontSize="30" FontWeight="SemiBold" Margin="0 0 0 20"/>

                <Grid Margin="0 0 0 20">
                    <Button Background="Transparent" BorderBrush="Transparent"
                            HorizontalAlignment="Left" Content="Войти"></Button>
                </Grid>

                <Grid Margin="0 0 0 10">
                    <TextBox x:Name="txtBoxLogin" Padding="10,8" FontSize="14"/>
                    <TextBlock Text="Логин" IsHitTestVisible="False" 
                    Margin="12,0,0,0" VerticalAlignment="Center"
                    Foreground="Gray" FontSize="14" 
                    Cursor="IBeam">
                        <TextBlock.Style>
                            <Style TargetType="TextBlock">
                                <Setter Property="Visibility" Value="Collapsed"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding Text, ElementName=txtBoxLogin}" Value="">
                                        <Setter Property="Visibility" Value="Visible"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </TextBlock.Style>
                    </TextBlock>
                </Grid>
                <Grid Margin="0 0 0 10">
                    <PasswordBox x:Name="txtBoxPassword" Padding="10,8" FontSize="14"
                                 PasswordChanged="txtBoxPassword_PasswordChanged"/>
                    <TextBlock x:Name="txtPasswordHint" Text="Пароль" IsHitTestVisible="False" 
                               Margin="12,0,0,0" VerticalAlignment="Center"
                               Foreground="Gray" FontSize="14" Cursor="IBeam">
                        <TextBlock.Style>
                            <Style TargetType="TextBlock">
                                <Setter Property="Visibility" Value="Visible"/>
                            </Style>
                        </TextBlock.Style>
                    </TextBlock>
                </Grid>
                <Grid Margin="0 0 0 10">
                    <PasswordBox x:Name="txtBoxPassword2" Padding="10,8" FontSize="14"
                                 PasswordChanged="txtBoxPassword2_PasswordChanged"/>
                    <TextBlock x:Name="txtPasswordHint2" Text="Повторите пароль" IsHitTestVisible="False" 
                               Margin="12,0,0,0" VerticalAlignment="Center"
                               Foreground="Gray" FontSize="14" Cursor="IBeam">
                        <TextBlock.Style>
                            <Style TargetType="TextBlock">
                                <Setter Property="Visibility" Value="Visible"/>
                            </Style>
                        </TextBlock.Style>
                    </TextBlock>
                </Grid>
                <Grid Margin="0 0 0 10">
                    <TextBox x:Name="txtBoxEmail" Padding="10,8" FontSize="14"/>
                    <TextBlock Text="Email" IsHitTestVisible="False" 
                        Margin="12,0,0,0" VerticalAlignment="Center"
                        Foreground="Gray" FontSize="14"
                        Cursor="IBeam">
                        <TextBlock.Style>
                            <Style TargetType="TextBlock">
                                <Setter Property="Visibility" Value="Collapsed"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding Text, ElementName=txtBoxEmail}" Value="">
                                        <Setter Property="Visibility" Value="Visible"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </TextBlock.Style>
                    </TextBlock>
                </Grid>

                <Button Margin="0 20" Padding="0 5" Content="Зарегистрироваться" 
                        Click="Button_Reg_Click"></Button>

            </StackPanel>
        </Border>
    </Grid>
</Window>

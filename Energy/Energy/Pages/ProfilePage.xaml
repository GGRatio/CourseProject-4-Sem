<Page x:Class="Energy.Pages.ProfilePage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Title="Профиль" 
      Background="White">

    <Page.Resources>
        <!-- Стиль для текстовых полей -->
        <Style TargetType="TextBlock">
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>

        <!-- Стиль для TextBox -->
        <Style TargetType="TextBox">
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="Padding" Value="10,8"/>
            <Setter Property="BorderBrush" Value="#DDD"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Margin" Value="10,20,0,0"/>
        </Style>

        <!-- Стиль для кнопок -->
        <Style x:Key="ProfileButton" TargetType="Button">
            <Setter Property="Width" Value="130"/>
            <Setter Property="Height" Value="40"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Cursor" Value="Hand"/>
        </Style>
    </Page.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>



        <!-- Карточка с данными -->
        <Border Grid.Row="1"
                Width="500"
                Height="450"
                Background="White" 
                CornerRadius="15"
                BorderBrush="#E0E0E0"
                BorderThickness="1"
                VerticalAlignment="Center"
                HorizontalAlignment="Center">

            <Border.Effect>
                <DropShadowEffect BlurRadius="10" ShadowDepth="2" Opacity="0.1"/>
            </Border.Effect>

            <Grid Margin="50">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="100"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <!-- Логин -->
                <TextBlock Text="Логин:" Grid.Row="0" Grid.Column="0"/>
                <TextBlock x:Name="txtLogin" FontWeight="SemiBold" 
                           Grid.Row="0" Grid.Column="1" Margin="10,0,0,0"/>

                <!-- Имя -->
                <TextBlock Text="Имя:" Grid.Row="1" Grid.Column="0" Margin="0,20,0,0"/>
                <TextBox x:Name="txtFirstName" Grid.Row="1" Grid.Column="1"/>

                <!-- Фамилия -->
                <TextBlock Text="Фамилия:" Grid.Row="2" Grid.Column="0" Margin="0,20,0,0"/>
                <TextBox x:Name="txtLastName" Grid.Row="2" Grid.Column="1"/>

                <!-- Телефон -->
                <TextBlock Text="Телефон:" Grid.Row="3" Grid.Column="0" Margin="0,20,0,0"/>
                <TextBox x:Name="txtPhone" Grid.Row="3" Grid.Column="1"/>

                <!-- Email -->
                <TextBlock Text="Email:" Grid.Row="4" Grid.Column="0" Margin="0,20,0,0"/>
                <TextBox x:Name="txtEmail" Grid.Row="4" Grid.Column="1"/>

                <!-- Кнопки -->
                <StackPanel Orientation="Horizontal" 
                            Grid.Row="5" Grid.ColumnSpan="2" 
                            HorizontalAlignment="Center"
                            Margin="0,40,0,0">

                    <Button x:Name="btnSave" Content="Сохранить" 
                            Style="{StaticResource ProfileButton}"
                            Background="#4CAF50" Click="btnSave_Click"/>

                    <Button x:Name="btnEdit" Content="Редактировать" 
                            Style="{StaticResource ProfileButton}"
                            Background="#2196F3" Click="btnEdit_Click"/>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</Page>

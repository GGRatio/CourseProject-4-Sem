<Page x:Class="Energy.Pages.GroupClassesPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Title="Групповые занятия"
      Background="{DynamicResource BackgroundBrush}">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Групповые занятия" FontSize="28" FontWeight="Bold" 
                   HorizontalAlignment="Center" Margin="0,0,0,20"/>

        <!-- Кнопки переключения -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,0,0,20">
            <Button x:Name="btnCurrent" Content="Текущие занятия" Click="btnCurrent_Click" 
                    Width="150" Margin="5" Background="{DynamicResource ButtonSuccessBrush}" Foreground="White"/>
            <Button x:Name="btnHistory" Content="История" Click="btnHistory_Click" 
                    Width="150" Margin="5" Background="{DynamicResource SecondaryBackgroundBrush}"
Foreground="{DynamicResource TextPrimaryBrush}"/>
        </StackPanel>

        <!-- Список занятий -->
        <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto">
            <WrapPanel x:Name="ClassesPanel" 
                       Orientation="Horizontal" 
                       HorizontalAlignment="Center"/>
        </ScrollViewer>
    </Grid>
</Page>